const SECOND = 1000,
    MINUTE = SECOND * 60,
    HOUR = MINUTE * 60,
    DAY = HOUR * 24,
    WEEK = DAY * 7,
    MONTH = DAY * 30;

module.exports = {
    SECOND,
    MINUTE,
    HOUR,
    DAY,
    WEEK,
    MONTH
}